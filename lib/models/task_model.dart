// thid class is going to be the model for the project to make the check boxes, where we will be able to add strings and bools to the list
// made a different class on another page for they easy access to edit

class TaskModel {
  String text;
  bool isComplete;

// constructor
  TaskModel({required this.text, required this.isComplete});
}
